
1. Entity теперь могут иметь инвентарь, в котором могут храниться объекты с компонентом Item.
Для этого, Entity должен иметь компонент Inventory, имеющий следующие поля:

- capacity: максимальное количество объектов в инвентаре;
- groups: список group id's (начинаются с #, согласно уже реализованной логике работы с групповыми id в редакторе). Если пуст, то инвентарь может хранить любые объекты. Если задан, то инвентарь может хранить только объекты, которые принадлежат хотя бы одной из этих групп.
- protected: булево значение, определяющее, защищен ли инвентарь от манипуляций со стороны других Actor (по умолчанию false). Если true, то другие Actor не смогут оперировать с предметами этого инвентаря, в том числе видеть содержимое. Полезно, к примеру, для защиты инвентаря NPC от игрока.
- items: список id объектов, которые находятся в инвентаре.

В API добавляются методы для работы с инвентарём, позволяющие добавить предмет (если он подходит по group id и есть свободное место), убрать предмет, проверить наличие предмета, получить список предметов.

Инвентарь персонажа игрока это теперь не частный случай, а работает по этой общей логике. Его команды типа TAKE или DROP теперь вызывают соответствующие методы API.

1. Любые объекты сцены теперь могут служить поверхностями для размещения предметов (Item). Для этого, объект должен иметь компонент Surface, имеющий следующие поля:

- capacity: максимальное количество объектов, которые могут быть размещены на поверхности;
- groups: список group id's. Если пуст, то поверхность может принимать любые объекты. Если задан, то поверхность может принимать только предметы, которые принадлежат хотя бы одной из этих групп;
- items: список id предметов, которые находятся на поверхности, вместе с их X/Y координатами на поверхности;

В API добавляются методы для работы с поверхностями, позволяющие добавить предмет (если он подходит по group id и есть свободное место), убрать предмет. При добавлении предмета на поверхность он размещается внутри области этого объекта (это может быть прямоугольная область, или многоугольник произвольной формы), прикрепляясь к каким-то случайным X/Y координатам в её предалах. При этом, если на поверхности уже есть предметы, то новый предмет по возможности не должен накладываться на них (только если нет другого выхода). Также предмет не должен выходить за пределы поверхности (если это невозможно, то такой предмет не может быть размещен на поверхности, даже если capacity позволяет).

1. Изменяетя логика команд типа TAKE чтобы они работали с инвентарем и поверхностями (из собственноого инвентаря, естественно, взять предмет нельзя, т.к. он уже взят, его можно только оттуда выбросить).
Добавляется стандартная команада PUT/DROP <item>. В ней можно указать предмет, цель куда класть, а также тип spatial отношения к цели (напр. PUT key UNDER THE TABLE), в этом случае в качестве цели ищеться поверхность, имеющая такое spatial отношение к указанному объекту. Без уточнения цели команда пытается положить указанный предмет (если он есть в инвентаре) на ближайшую к персонажу поверхность, с которой он может быть размещен (с учетом group id и capacity). Положить можно только на поверхность, доступную для персонажа (которую он может видеть и которая при этом не заблокирована и находится рядом с персонажем, по той же логике, что  комаде TAKE или EXAMINE).
 При этом, если открыта Subscene, то в первую очередь кандидатами на цель должны рассматриваться поверхности их Subscene.  Нужно также предусмотреть сообщения о том, что предмет положить невозможно с указанием причины (нет такого места ).
Команда PUT/DROP также может использоваться с предметами, имеющими инвентарь. Например: PUT cassete into tape recorder. В этом случае, предмет размещается уже не на поверхности, а в инвентаре  объекта-контейнера, если, конечно, это возможно соглавно выше описанным правилам. На этот случай также понадобятся текстовые сообщения в TA про успешность/неуспешность операции.
Положить в контейнер персонаж игрока может только если тот видим и доступен для него. Если же контейнер находится в Subscene, то достаточно лишь того, что он виден.

Если целевой объект размещён в инвентаре персонажа, он всегда видим и доступен для подобных  опереаций.

Никакие команды не должны оперировать с disabled объектами. С точки зрения игры таких объектов не существует.
