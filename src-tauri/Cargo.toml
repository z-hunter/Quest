[package]
name = "scanline_desktop"
version = "0.1.0"
description = "Desktop shell for the Scanline engine"
authors = ["Quest Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
base64 = "0.22.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "=2.11.1", features = [] }
notify = "6"
reqwest = { version = "0.12", features = ["json"] }

[features]
default = []
