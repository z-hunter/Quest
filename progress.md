Original prompt: Давай временно переключимся с парсера к архитектуре приложения и придумаем способ автоматического управления загруженными сценами, чтобы не получалось так, что каждая открытая сцена остаётся в RAM. Возможно, разумно хранить несколько наиболее часто загружаемых сцен, а редко загружаемые из памяти выгружать. И вообще мониторить кол-во памяти занятое сценами, и регулировать кол-во сцен в кэше в зависимости от этого

- Playwright also panned the rotated prepared `cube` diagonally; its roof and side faces changed by ordinary parallax as one welded rigid body, with no seam break or inverse morph.

- 2026-08-21: Box3D follow-up in progress. Scene now owns `box3dPerspective`; managed faces keep physical XYZ/P but bypass the second Quad camera-parallax pass. Added Canvas2D BSP fragment ordering/clipping and shared 3D raycast hit resolution. Focused Box/render tests and typecheck pass.
- 2026-08-21: copy/duplicate/prefab payload v3 recursively includes spatial descendants, managed Box faces, nested folders, and folder contents; remaps spatial/folder references, restores exact Box faces without defaults, and selects copied roots only. Focused hierarchy tests and typecheck pass.
- 2026-08-21: Fixed Box3D off-axis deformation and opaque occlusion seams. Projection now separates the depth-projected Box center from vertex-local 3D offsets, so moving a Box or Camera in X/Y translates the solid without shearing it; Move Z still changes scale and local perspective. Opaque Box faces and BSP clips receive coverage-only overlap while UV geometry and translucent alpha paths stay unchanged. Focused tests, typecheck, and Playwright checks for three identical translated shapes and a fully occluded nested Box pass.
- 2026-08-21: Added Scene `3D View Turn` (default 0.5). A bounded off-axis correction makes rear Box3D vertices follow Camera XY more slowly than front vertices without restoring unbounded corner distortion; Scene settings resync Box faces directly in the render path.
- 2026-08-21: Fixed managed Box3D face selection outlines. SceneEditor now uses the Quad `getVisualVertices()` path instead of reapplying vertex parallax to already camera-projected Box faces; Playwright confirms the green outline matches centered and off-axis faces.
- 2026-08-21: Fixed point-click selection leaking through Box3D backface culling. Regular clicks now consume the shared 3D raycast resolver instead of rerunning legacy Entity polygon hit-tests; marquee selection intentionally still includes culled managed faces. Focused tests, typecheck, and Playwright clicks on three visible faces pass.
- 2026-08-21: Superseded per-Box `3D View Turn` with one Scene pinhole projection. Every Box3D vertex now projects only from absolute world XYZ and Camera XY, so touching vertices owned by different Boxes remain identical and Camera movement reveals depth consistently. Scene `3D Perspective` remains the configurable strength.
- 2026-08-21: Replaced the rectilinear Box3D lens with one shared angular projection. `atan2` bounds off-axis depth shear while preserving common world vertices, camera-driven face turning, physical depth P, and exact inverse raycast hit testing.
- 2026-08-22: Removed the angular Box3D lens after it caused nonlinear edge flattening. Restored a standard rectilinear camera and tied its world-space focal distance to `1 / CameraZoom`; zoom-out now dollies the 3D camera back instead of widening its FOV, preventing the extreme edge deformation seen at zoom 0.5.
- 2026-08-22: Box3D face outward winding is now separate from the managed Quad's UV corner order. Each numbered face gets an upright outside-view texture basis, fixing the inherited 90-degree rotation and mirroring without changing culling, BSP, or raycast geometry.
- 2026-08-22: Box3D keeps its full two-sided shell in the BSP batch. Disabled/no-fill faces and texture alpha now reveal the other faces' inside surfaces; textured faces no longer receive an opaque color underlay.
- 2026-08-22: Managed Box3D faces now support `3d-parallax` Entity bindings through exact camera-ray/physical-plane sampling. Bound Entity billboards join the Box3D BSP at the face's effective Layer, persist their front/back surface side, remain behind an opaque reversed face but show through alpha, and inherit Disabled from their face/Box without losing authored or serialized state. Playwright verified side switching, transparent/opaque coverage, UI and Disabled inheritance; focused tests, all 850 tests, typecheck and build pass.
- 2026-08-22: Unsplit Box3D faces now render directly through `Quad.render()` without the redundant outer BSP clip; only polygons actually cut by BSP retain fragment clipping. This is a render-path simplification, but the later textured-face reproduction proved it was not the cause of internal triangle seams.
- 2026-08-22: Fixed textured Quad mesh seams visible on Box3D faces. The old vertex-by-vertex triangle expansion did not guarantee perpendicular coverage on long/skewed edges, allowing the Box's rear shell to show through. Coverage now scales each triangle about its centroid from its minimum altitude, guaranteeing the requested edge overlap while leaving UV transforms unchanged. Playwright compared a sprite-textured Box face (rear shell enabled) against an equal normal Quad; focused tests, typecheck, and build pass.
- 2026-08-23: Added direct Box3D editor manipulation. Ctrl-clicking a managed face selects its parent; parent selection shows all physical top/bottom vertices and occlusion-aware XYZ axes. Mouse modifiers cover XY/Z movement, XYZ rotation, scale, frustum dimensions and top offsets, while dragging a selected face moves the whole Box in that face's physical plane. Focused tests, typecheck and Playwright gesture/visual checks pass.
- 2026-08-23: Corrected Box3D axis occlusion: the camera-side segment is now drawn over the visible face up to its exact 3D shell intersection, while the portion inside and behind the solid stays occluded.
- 2026-08-23: Fixed prism/pyramid Box3D occlusion. Managed faces now enter BSP/raycast with consecutive duplicate vertices removed; zero-area faces are skipped and collapsed Quads become real triangles, preventing a degenerate top plane from flattening the BSP and drawing rear faces over front ones.
- 2026-08-23: Added `3dSys.md`, the developer contract for Box3D and the shared 3D Scene: coordinate spaces, transform/projection math, managed Quad bridge, BSP/alpha/degenerate geometry, raycast, surface anchors, editor lifecycle, invariants, limitations, code map and tests. Updated `tech-spec.md` navigation and added the design-level Box3D object section to GDD.
- 2026-08-23: Reconciled Box3D BSP-limit rendering with point hit-test fallback, replaced the orthographic `-1e9` ray origin with a polygon-derived lower bound, and documented/fixed stage-space pivot axis overlays. Added focused boundary regressions. Validation: focused 19 tests, typecheck, full 859 tests, build, and Python Playwright visual smoke on `box3d2` pass; the Node skill client still cannot resolve its Playwright package.

- Решение: вводим Scene Registry + adaptive scene cache.
- Scene cache policy: единая для runtime и editor.
- Budget: по estimated scene weight, не по browser memory API.
- UI: в нижней строке editor слева показывать `MEM x | y`, где `x` — estimated memory, `y` — число сцен в cache.
- Важно не трогать локальные артефакты пользователя: `public/scenes/bug.json`, `public/scenes/ttt.json`, `public/scenes/test_room.json`, `public/text/scenes/new_scene.json`, `tasks.md`, `.nvimlog`.
- Реализовано: SceneManager разделён на registry + cache metadata; добавлены estimated weight, eviction, cache stats и registry scan по `public/scenes`.
- Реализовано: `game.goTo()` теперь ищет сцену через `sceneRegistry`, а не только среди живых scene instances.
- Реализовано: bottom menu editor показывает `MEM x | y` слева.
- Реализовано: save/save-as синхронизирует scene registration, чтобы cache/registry не теряли сцену после смены id.
- Проверки: `npm run -s typecheck` и `npm run -s build` проходят.
- Ограничение среды: локальный `npm run dev` smoke test в этой сессии блокируется sandbox-ошибкой `vite -> esbuild spawn EPERM`, поэтому браузерный прогон не был надёжно выполнен.
- Новое: добавлен debug-profiler сцен в `SceneManager`.
- `profileCurrentSceneMemory()` снимает snapshot по текущей сцене: weight units, heap snapshot, texture estimate, bytes-per-unit.
- `profileScenes([...])` прогоняет серию сцен, по очереди загружает их и печатает `console.table(...)` с `deltaMb`, `textureMb`, `kbPerUnit`.
- Debug API проброшен в `window.__QUEST_DEBUG__`:
  - `__QUEST_DEBUG__.profileCurrentSceneMemory()`
  - `__QUEST_DEBUG__.profileScenes([...])`
  - `__QUEST_DEBUG__.game`
- Browser smoke (MCP): `__QUEST_DEBUG__.profileCurrentSceneMemory()` и `__QUEST_DEBUG__.profileScenes(['test_room'])` отрабатывают, новых console errors нет.
- Новый этап: scene cache и image cache сведены в согласованную texture-first модель.
- Scene weight теперь должен доминирующе учитывать texture bytes, а старый graph-weight используется как малый корректирующий вклад.
- Budgets scene cache подняты в 3 раза; image cache получил отдельный budget по device class.
- В `GDD.md` в конец раздела `Техническая реализация` добавлено описание profiler и примеры вызова через `window.__QUEST_DEBUG__`.
- Проверка browser loop: skill client не запустился, потому что в окружении нет пакета 'playwright'; для smoke test использован встроенный browser MCP.

- New task: make editor bottom menu modifier-aware (Ctrl/Alt/Shift), with dynamic labels such as D ENABLE/DISABLE based on selected object state.
- Implemented state-driven EditorBottomMenu with modifier modes; added Alt/Shift Save As path and Ctrl action set; pending browser smoke verification.

- Verified in browser: bottom menu now switches between base / Alt / Ctrl / Shift layouts, and Alt mode updates D from Disable to Enable after toggling selected object disabled state.\n
- Selection slots implemented: Shift+1/2 saves current selection; 1/2 restores it; empty slot shows toast.\n
- Refactored Game.ts: extracted InventoryManager and GameSemanticAPI to src/systems/; added AnyComponent union type for strict component safety.
- Implemented universal Exit and Entry components: works on any object, supports same-scene teleport, automated collision-based actor transfer.
- Implemented instant camera snap: camera now perfectly follows player during transitions without smoothing delay.
- Infrastructure: Manually installed Playwright MCP and skill to .gemini directory (requires session restart to activate).

- 2026-07-02: implemented passive observed-Actor action events. `perceptionRadius` moved from NPC component to Actor, player sees nearby foreign actions as bracketed console lines, and observed actions no longer wake PM.
- Added structured/localized LOOK/EXAMINE, OPEN/CLOSE, TAKE/PUT, USE/COMMAND and Exit observation messages; SceneLog now stores `knownByActorIds` with legacy migration.
- Validation: typecheck passes; focused observed-action/SceneLog/Exit tests pass; full suite is 525 passed with the same 4 pre-existing PM failures.
- Browser loop caveat: Vite started successfully, but the required skill client could not launch because package `playwright` is not installed; the app also exposes neither `window.render_game_to_text` nor `window.advanceTime`, so deterministic parser-scene verification still needs dedicated instrumentation.

- 2026-07-27: implemented textured Quad rendering. Quad now reuses `spriteName`/Animator, supports `stretch` and independently-scaled `tile` UV mesh modes, and defaults to homography-based texture perspective. Texture replaces fill/checkerboard while Retro Grid remains above it. Added properties UI, GDD/vault documentation, and focused geometry/render tests. Browser loop remains blocked because the local `playwright` package is not installed.
- 2026-07-27: fixed Canvas2D hairline seams in textured Quad meshes by expanding each affine triangle by 0.75 screen pixel and clipping the combined result to the original Quad boundary. Focused Quad and snapping tests pass; browser loop is still blocked by the missing local `playwright` package.
- 2026-07-27: replaced source-resolution-proportional Quad tessellation after it caused severe FPS loss. Stretch now uses one cell rendered as a four-triangle fan through the homography center (four draw calls total), removing the single-diagonal paper-fold behavior. Tile still creates one cell per repeat. Seam overlap is 1.25 screen pixels, capped at 8% of the triangle's shortest edge.
- 2026-07-27: replaced the fixed textured-Quad fan with bounded screen-space mesh adaptation (maximum 32 triangles), independent of source sprite resolution. Nearly affine Stretch Quads now use a single flat affine draw. Seam coverage expands only triangle clips; their UV transforms remain calculated from original vertices to prevent overlap-induced texture mismatch. Typecheck, focused Quad tests, full test suite, and production build pass. Browser skill client remains unavailable because the local `playwright` package is missing.
- 2026-07-27: Retro Grid perspective now rejects homographies for concave, crossed, and collapsed Quads, including transforms whose projective horizon enters the unit square. Those shapes use the existing bounded bilinear fallback. Checkerboard and internal grid strokes are additionally clipped to the Quad contour, matching textured-Quad containment, so malformed editor geometry cannot emit screen-wide rays. Focused Quad tests and typecheck pass; browser skill client remains unavailable because the local `playwright` package is missing.

- 2026-07-30: 3D Perspective Walk follow-up. Replaced per-frame edge-distance interpolation for vertical movement with a continuous ray from the actor to the current visual intersection of the Quad's projected left/right side lines. Actor and per-vertex-parallax Quad geometry are projected into the same visual coordinate space. Focused tests cover ray continuity and camera-projected vanishing-point direction (8 passed); typecheck passes. Browser verification in `logo` traced 12 upward movement samples: the actor remained collinear with the current visual vanishing point until reaching the Quad's top boundary, with no console errors; the inspected screenshot rendered correctly.

- 2026-08-11: implemented Unified Surface Depth. Quad now resolves perspective-corrected local surface coordinates through the same projection used by Retro Grid, and uses them for interpolated P, local movement axes, and Depth scaling controller scale. `3d-parallax` keeps Actor/Static visually fixed while updating P; Actor speed is uniformly P-based for keyboard, click routes, and NPC routes, while `3d-perspective walk` only changes keyboard direction. Added `Depth scaling controller` (Min/Max, last matching visible Quad wins), effective non-mutating Quad geometry, default `ignoreScaling=true` for new/legacy Quads, editor controls, GDD, and regressions. Full `npm test`, typecheck, build, and diff check pass. Browser skill client remains unavailable because local `playwright` is absent.
# 2026-08-11 — Legacy Quad depth-scaling migration

- Added a `depthScalingVersion` save marker for Quad. Old scenes now force Quad `ignoreScaling=true` on load, preserving their authored geometry even when their inherited legacy JSON stored `ignoreScaling: false`.

- 2026-08-11: fixed numeric input handling in `Depth scaling controller`. Numeric label scrubbing now selects a number field owned by its label before inspecting siblings, so `Min` no longer edits `Max`; direct input/spinner interactions are excluded from scrubbing and retain native focus/editing.
- Validation: `npm run typecheck` and focused numeric/UI tests pass. Browser skill client remains unavailable because the local `playwright` package is missing.

- 2026-08-11: stabilized `3d-perspective walk` and 3d-parallax surface tracking. Perspective keyboard axes now turn off in a small edge zone so adjacent Walkbox regions cannot alternate incompatible directions; normal input can reach/cross the boundary. 3d-parallax now stores runtime surface `(u,v)` and reconstructs its visual point through the Retro Grid resolver after camera movement, preventing Actor/Static from drifting relative to grid lines. Focused movement/parallax regressions pass.
- Surface tracking is additionally run in deterministic Scene phases immediately after camera movement and after entity movement, removing dependence on Quad/Actor ordering in `scene.entities`.

- 2026-08-11: fixed Player/Actor snapping near a `3d-parallax` Quad edge with P=1. Camera auto-center now resolves Player position in screen space with P, eliminating camera/P feedback. Actor route targets and waypoints preserve their visual position whenever the surface updates P. Added camera and route regressions.
- Follow-up root cause: `Scene.isWalkable` evaluated Actor with `collisionCamera` while Quad WalkBox geometry used the smoothed render camera. Quad WalkBox collision now consistently uses render camera, preventing a false invisible boundary during auto-center. Added regression coverage.

- 2026-08-11: fixed a second, camera-independent Actor snap at dynamic Quad/WalkBox edges. The terminal route step previously assigned the route point directly when it was closer than one step, bypassing `isWalkable()`. If a 3d-parallax surface boundary changed since route planning, mouse or keyboard movement could therefore place the Actor inside the lower edge and trap it. Terminal steps now use the same walkability guard and stop with `route_blocked`; a focused regression covers the bypass. Focused test, typecheck, full `npm test`, and diff check pass. Browser verification remains blocked because the local `playwright` package is missing.

- 2026-08-11: `t-quad` exposed the remaining lower-edge P=1 snap: the perspective surface inverse used a forward finite difference at `u/v = 1`, producing a zero derivative. Newton iteration could then clamp an interior point to `v=1`, assign P=1 prematurely, and subsequently snap it to the lower grid edge. The inverse now uses central boundary-safe derivatives with a damped, improving-only Newton step. Regression covers an interior point on the `t-quad`-style lower perspective slope; it retains P below 1. Focused tests, typecheck, full suite (69 files / 771 tests), and diff check pass.

- 2026-08-11: fixed editor normalization bypassing Quad `Depth scaling controller` precedence. Toggling scene Depth Scaling previously recalculated every object's base dimensions with raw `scene.getScaling(y)`, so controller-managed objects baked in the scene factor and were then scaled by the controller again. Scene toggle, Transform editing, and the Disable Depth-scaling property path now use `scene.getDepthScaleFor()` exclusively. Regression verifies controller-managed dimensions remain unchanged when scene scaling is turned off. Typecheck and full suite pass (69 files / 772 tests). Browser client remains unavailable because the local `playwright` package is missing.

- 2026-08-12: added Quad `Depth sort mode: By Parallax`. At the same Layer, a Quad in this mode uses its global P as its plane depth: lower-P objects render behind it and higher-P objects render in front, independent of their Y values; equal P falls back to existing sorting. Added editor option, tooltip/GDD documentation, and renderer regression. Typecheck and full suite pass (69 files / 773 tests). Browser client remains unavailable because the local `playwright` package is missing.

- 2026-08-12: changing global Quad Parallax in the editor now preserves every visual vertex. Because global P multiplies local vertex P, compensation shifts each authored vertex independently before changing the global value; P therefore changes visible position only when the camera subsequently moves. Bound vertices are also stable: their resolver now converts target effective P and visual position into the source Quad's global-P coordinate space instead of overwriting the compensation on the next frame. Added regressions for mixed vertex P and a bound vertex with non-zero camera, plus GDD contract. Focused tests and typecheck pass. One full-suite run hit an unrelated fake-timer flake in `puppet-master.test.ts`; its immediate isolated rerun passed all 137 tests. Browser client remains unavailable because the local `playwright` package is missing.

- 2026-08-12: corrected the global-Quad-P compensation after a Playwright reproduction in `t-quad`. The previous implementation kept Quad's intermediate `getVisualVertices()` fixed, but SceneRenderer then applies a second global-P camera transform, so the rendered Quad still moved. Compensation now preserves the final canvas coordinate (`vertex - camera * localP * globalP`). Direct and Retro Grid bindings use that same final-render coordinate system. Python Playwright changed `Quad_408` from P=1 to P=0.5 and measured all four final screen vertices unchanged; focused Quad tests and typecheck pass.

- 2026-08-12: fixed Quad viewport culling for global P != 1. Quad.render uses inner vertices but SceneRenderer subsequently applies an outer camera transform at global P; the culling viewport must therefore be centered on camera * globalP, not raw camera. Added a focused regression for P=0.5 at a far camera position; focused Quad tests and typecheck pass.

- 2026-08-12: fixed rebinding of an already vertex-bound Quad vertex to a Retro Grid node. Editor mutual vertex bindings left a stale reciprocal link on the previous target; the next binding resolver pass could restore the old vertex relationship. Rebinding now removes only the matching reciprocal link before storing the new vertex/grid binding. Added editor regression; focused tests (26) and typecheck pass. Playwright reproduced the real editor path on `t-quad`: `Quad_408` vertex 0 changed to a Grid binding and the old `Quad_481` reciprocal binding was removed.

- 2026-08-12: removed the remaining artificial two-vertex limit. Vertex bindings are directed: a vertex has one outgoing binding, but any number of vertices may bind to the same target vertex or Grid node. The editor no longer creates a forced reverse binding that overwrites an existing incoming connection. Added a three-vertex regression; 27 focused tests and typecheck pass. Playwright confirmed two Quad vertices retain bindings to the same target after updates.

- 2026-08-12: fixed rebinding of a connected Quad group. During a group drag, vertices belonging to that same connected component move together and remain coincident with the old target; snap selection previously considered them first, so the old binding won and the new connection was never recorded. `EditorSnappingSystem.snapVertex()` now accepts the connected vertex references to exclude, and `EditorTransformManager` supplies them from `QuadObject.getConnectedVertices()`. Added a four-vertex chain regression; editor tests (33) and typecheck pass. Playwright confirmed a moved group selects the external target when old and external targets overlap.

- 2026-08-12: completed connected-group rebinding. On release, the new vertex/Grid binding is now assigned to every member of the moved connected component, not only the dragged vertex. Existing reciprocal links are cleared first; incoming links remain unrestricted. Added a four-vertex onMouseUp regression; editor tests (34) and typecheck pass. Full Playwright drag confirmed all four Quad vertices bind to the external target.

- 2026-08-13: investigation of the nearly collapsed 3d-parallax surface regression. Browser reproduction in `t-quad`: with `Quad_408` approaching zero visual height (`camera.y≈217`), `Static_923` loses its runtime `__surfaceParallaxBinding` and restores P=1. The tracking path reconstructs a grid point and immediately re-inverts it through `getSurfaceMetricsAt()`; the near-degenerate visual polygon makes containment/inversion unstable, so the valid binding is treated as outside the Quad. No fix applied yet; preserve stored `(u,v)` while the surface is degenerate instead of re-classifying it through the collapsed projection.

- 2026-08-13: fixed the near-collapsed Quad projection regression. `projectQuadGridPoint()` now smoothly fades the projective contribution to the existing bilinear mapping when visual Quad compactness falls below 4% (off at 1%), preventing the projective X singularity for Retro Grid, texture mesh, and 3d-parallax alike. `ThreeDParallaxSystem` additionally retains a known `(u,v)` binding while that visual surface is collapsed, instead of falsely unbinding it. Focused tests, typecheck, and full `npm test` pass. Python Playwright reproduction in `t-quad` across camera y=200..218 keeps `Static_923` P and binding stable and matches its Retro Grid point without console errors. The skill's Node Playwright client remains unavailable because its runtime cannot resolve the `playwright` package; Python Playwright was used for the browser check.

- 2026-08-14: follow-up investigation after the visual Quad flips through zero area. The exact fault is a non-unique inverse in the unstable band, not P: at `t-quad` camera y=217, 76/81 sampled interior points fail `getSurfaceMetricsAt(getGridPointAt(u,v))` (returned alternate `(u,v)` or null); at y=220 and later all samples resolve correctly again. The existing 3d-parallax collapse fallback only runs when the stability factor is exactly zero and, worse, still accepts a non-null but incorrect inverse. Correct fix: while a *known* surface binding's visual projection is unstable (stability < 1, the same 1%-4% hysteresis band used by the projector), never re-invert it. Reconstruct from stored `(u,v)` and use `getParallaxAtGrid`; re-enable normal inverse only after full stability returns. New objects should not attach in this band because their position is mathematically ambiguous.

- 2026-08-14: implemented the Quad-inversion fix. `isVisualSurfaceUnstable()` exposes the whole shared projection transition band. For an existing 3d-parallax binding, `ThreeDParallaxSystem` now bypasses the ambiguous inverse entirely, preserving stored `(u,v)` and calculating P directly from `getParallaxAtGrid`; normal hit/inverse resolution resumes after full stability. A focused regression uses a point whose inverse is known to be wrong at camera y=218 and verifies its binding remains unchanged. Focused tests and typecheck pass; browser `t-quad` check confirms stable binding through y=218 and restoration at y=240. Full suite had one unrelated fake-timer overflow in `puppet-master.test.ts`; immediate isolated rerun passed all 137 tests.

- 2026-08-14: prevented camera-only surface acquisition. An unbound object now records its last world position after its first evaluation; 3d-parallax can newly bind it only when that world X/Y changes, never merely because camera movement makes its screen position overlap a Quad. This specifically fixes `test_t`: moving camera through the inverted `floor` and back leaves `wall` at x=19/y=-1/P=0.6 without a surface binding, while existing `chair` tracking remains intact. Focused parallax tests and typecheck pass.

- 2026-08-14: applied the same ownership rule to `Depth scaling controller`. `Scene.getDepthScaleFor()` records a controller and corrected depth coordinate when an object is first evaluated or actually changes world X/Y; camera-only movement can neither acquire nor replace the controller. A valid binding keeps its stored depth through visual Quad inversion, and moving out releases it. Regression covers outside -> camera movement -> real entry -> real exit. Typecheck, diff check, and full suite (69 files / 793 tests) pass; visual `test_t` check captured in `playwright-depth-controller-stable.png`.

- 2026-08-14: added Entity `Depth Sort mode` for Static and Actor. `By Y` remains the compatible default; `By Parallax` reuses the shared equal-Layer P comparison, while `Manual` compares only Layer and preserves scene order at equal Layer. The setting is serialized as `depthSortMode`, exposed in Entity Transform properties, and documented in GDD. Focused renderer tests and typecheck pass. Full suite reached 794/795 tests but hit the known unrelated `puppet-master` fake-timer flake; immediate isolated rerun passed all 137 tests. Visual `test_t` screenshot captured in `playwright-entity-depth-sort.png`.

- 2026-08-14: fixed the `puppet-master` fake-timer flake. `ActorPlanExecutor.watchMoveCompletion()` had left its recurring MOVE_TO polling timeout outside `pendingTimeouts`, so executor cleanup could not cancel it and a later fake-timer test could run it indefinitely. Polling and delayed move completion now use the same tracked-timeout helpers; polling also stops if the Actor no longer belongs to a scene. Regression covers clearState, clearAllPending, Actor stop, and scene removal. Isolated PM tests (138), typecheck, full suite (69 files / 796 tests), and diff check pass.

- 2026-08-20: implemented editor-only baked `3D Rotate Y` for multi-selected Quads. The rotation reconstructs authored vertex P as inverse depth using `F = design width / 2`, rotates around an ephemeral world `Axis X / Axis P`, rejects vertices at/behind the virtual camera, and propagates the resulting effective P/X/Y to connected vertices outside the selection. The editor renders a yellow vertical axis guide to the first selected-Quad intersection. Focused tests, typecheck, full suite (75 files / 833 tests), and Playwright on `q-quad` (including unselected bound Quad stitching) pass.

- 2026-08-20: corrected the 3D rotation axis guide. It is now hidden for a single Quad and finds its endpoint by intersecting the vertical Axis X/Axis P line with each selected Quad reconstructed in inverse-depth 3D, rather than with the projected contour. A trapezoid regression confirms P=0.75 intersects between P=1 and P=0.5 edges; Playwright confirms the single-selection guide is absent and multi-selection guide is shown.
